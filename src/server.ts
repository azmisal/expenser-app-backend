import { buildApp } from "./app";
import "dotenv/config";

const app = buildApp();

const start = async () => {
    try {
        await app.listen({ port: 3000 });
    } catch (err) {
        app.log.error(err);
        process.exit(1);
    }
};

start();